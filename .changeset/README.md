# Changesets

This folder holds changeset files used to version and publish `nosleep-cli`.

- Add a changeset with `bun run changeset`.
- Merging the generated release PR publishes npm, creates a GitHub Release, and updates Homebrew.
